[project]
name = "bee-mcpb"
version = "0.6.10"
description = "Bee MCP bundle launcher - runs the hosted bee-mcp server from bee-sdk"
requires-python = ">=3.10"
dependencies = [
    "bee-sdk>=0.6.10",
]
